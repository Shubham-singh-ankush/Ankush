export function getImageUrl() {
    // Optionally, use environment variable for the base URL
    // const baseUrl = 'http://127.0.0.1:8000/';
    const baseUrl = 'https://cog.vcanaglobal.ga/public/';
    return `${baseUrl}`;
  }